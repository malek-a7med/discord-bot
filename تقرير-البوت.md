# 📋 تقرير شامل — بوت زنجي
**تاريخ التقرير:** 25 يونيو 2026  
**حالة البوت:** ✅ شغال بالكامل

---

## 1. نظرة عامة على البوت

| الخاصية | التفاصيل |
|---|---|
| **اسم البوت** | زنجي |
| **المنصة** | Discord |
| **اللغة** | Node.js 22 |
| **المكتبة الأساسية** | discord.js v14.26 |
| **عدد الأوامر** | 59 أمر |
| **نوع الأوامر** | Slash Commands (/) |
| **لغة الواجهة** | عربي بالكامل |

---

## 2. المطورون (الأونرز)

| الاسم | Discord ID |
|---|---|
| مالك | 954816748140503090 |
| مصطفى | 1448840687763325018 |
| عمر | 844490366614110218 |
| أوفيكس | 594250288282730551 |

---

## 3. البنية التقنية

### المكتبات الأساسية

```
discord.js        v14.26.4   — مكتبة Discord الرئيسية
distube           v5.2.3     — نظام الموسيقى
@distube/yt-dlp   v2.0.1     — تشغيل YouTube عبر yt-dlp
@distube/spotify  v2.0.2     — دعم روابط Spotify
@distube/soundcloud v2.0.4   — دعم SoundCloud
@google/generative-ai v0.24.1 — Gemini AI
canvas            v3.2.3     — رسم الصور (بطاقات الرانك وغيرها)
express           v4.22.2    — سيرفر HTTP للـ Keep-Alive
genius-lyrics     v4.4.7     — كلمات الأغاني
tesseract.js      v7.0.0     — قراءة النصوص من الصور (OCR)
```

### المتغيرات المطلوبة (Secrets)

| المتغير | الحالة | الاستخدام |
|---|---|---|
| `DISCORD_TOKEN` | ✅ موجود | تسجيل دخول البوت |
| `GOOGLE_API_KEY` | ✅ موجود | Gemini AI |
| `GOOGLE_CLIENT_ID` | ⚠️ اختياري | Google Drive |
| `GOOGLE_CLIENT_SECRET` | ⚠️ اختياري | Google Drive |
| `GOOGLE_REFRESH_TOKEN` | ⚠️ اختياري | Google Drive |
| `YOUTUBE_API_KEY` | ⚠️ اختياري | YouTube API |

### هيكل الملفات

```
/
├── index.js                  — نقطة الدخول الرئيسية (4933 سطر)
├── config.js                 — إعدادات البوت والأونرز
├── package.json
├── commands/
│   ├── music.js              — نظام الموسيقى الكامل
│   ├── games.js              — الألعاب (روليت، مافيا، XO)
│   ├── codenames.js          — لعبة كلمات سرية
│   ├── party-games.js        — الهاتف المكسور + صنع الميم
│   ├── polls.js              — نظام الاستفتاءات
│   ├── games-hub.js          — مركز الألعاب + /احدث-المميزات
│   ├── bank-life.js          — البنك الافتراضي (الحياة)
│   ├── bank-luck.js          — البنك (الحظ - عالمي)
│   ├── bank-luck-eg.js       — البنك (الحظ - مصري)
│   ├── bank-savings.js       — نظام الادخار
│   ├── game-shop.js          — متجر الألعاب
│   ├── daily-challenge.js    — التحدي اليومي
│   ├── quiz.js               — مسابقات
│   ├── image-cleaner.js      — تنظيف الصور
│   ├── quick-clean.js        — تنظيف سريع
│   └── translator.js         — مترجم
├── helpers/
│   ├── music-card.js         — واجهة الموسيقى (Dashboard + أزرار)
│   ├── music-handler.js      — معالج أحداث DisTube
│   ├── auto-mod.js           — الحماية التلقائية
│   ├── moderation-listener.js — مستمع الإشراف
│   ├── bot-memory.js         — ذاكرة البوت
│   ├── gemini-keys.js        — إدارة مفاتيح Gemini
│   ├── gemini-multimodal.js  — Gemini متعدد الوسائط
│   ├── owner-ai.js           — AI خاص بالأونر
│   ├── rank-roles.js         — أدوار الرانك
│   ├── single-instance.js    — منع تشغيل نسختين
│   ├── image-cleaner-api.js  — API تنظيف الصور
│   ├── google-drive.js       — Google Drive
│   ├── google-docs.js        — Google Docs
│   └── drive-utilities.js    — أدوات Drive
└── data/
    ├── server_database.json  — قاعدة بيانات السيرفر
    └── gemini-keys-extra.json — مفاتيح Gemini الإضافية
```

---

## 4. الأنظمة الرئيسية

### 🎵 نظام الموسيقى
- **المكتبة:** DisTube v5 + YtDlpPlugin + SpotifyPlugin + SoundCloudPlugin
- **البحث النصي:** YouTube مباشرة عبر yt-dlp → SoundCloud كـ fallback
- **روابط Spotify:** SpotifyPlugin يتعامل معها بـ scraping mode (بدون API)
- **روابط YouTube:** yt-dlp مباشرة
- **روابط SoundCloud:** SoundCloudPlugin
- **Dashboard:** بطاقة تفاعلية مع أزرار التحكم الكاملة

أزرار الـ Dashboard:
| الزرار | الوظيفة |
|---|---|
| ⏸️ وقف / ▶️ كمّل | إيقاف مؤقت / استئناف |
| ⏭️ التالية | تخطي الأغنية |
| ⏹️ اطلع | إيقاف كامل + مغادرة القناة + حذف الداش بورد |
| 🔁 تكرار | تبديل وضع التكرار |
| 🔊+ / 🔊- | رفع/خفض الصوت |

### 🤖 نظام Gemini AI
- **المهام:** محادثة عامة، إجابة أسئلة، تحليل صور، مساعدة أونر
- **تعدد المفاتيح:** يدعم مفاتيح متعددة مع rotation تلقائي
- **التخزين:** `data/gemini-keys-extra.json`
- **طرق الكلام:** عادي / حر / سام (خاص بالأونر)

### 🛡️ نظام الحماية التلقائية (Auto-Mod)
- **Two-Pass Scan:**
  1. EXTREME_REGEX — حظر فوري
  2. SUSPICIOUS_REGEX + Gemini للتأكيد (throttle 5 ثواني)
- **الإجراءات:** تحذير → ميوت → كيك → باند
- **تأكيد الإجراءات:** confirmة للمود في 90 ثانية عبر أزرار

### 🎮 الألعاب
- **روليت، مافيا، XO** — in-memory Maps
- **كلمات سرية (Codenames)** — بورد 5x5، spymaster عبر DM
- **الهاتف المكسور** — لعبة جماعية
- **صنع الميم** — بناء ميمات جماعية
- **استفتاءات** — activePolls Map، تنتهي تلقائياً

### 💰 نظام البنك
- **البنك الافتراضي (الحياة):** رصيد، تحويلات، سحب، إيداع
- **بنك الحظ (عالمي + مصري):** ألعاب حظ
- **بنك الادخار:** ادخار مع فوائد
- **متجر الألعاب:** شراء قدرات خاصة (gameAbilities)

---

## 5. كل المشاكل اللي حصلت وحلولها

---

### 🔴 المشكلة الأولى: البوت مش شغال على Replit أصلاً

**السبب:** المشروع لم يكن مُعد للعمل على Replit — كانت ناقصة حزم npm، system dependencies، وإعدادات البيئة.

**الحل:**
1. تثبيت كل حزم npm: `npm install`
2. تثبيت system dependencies من خلال Nix:
   - `ffmpeg` — تشغيل الصوت
   - `cairo`, `pango`, `librsvg`, `pkg-config` — مكتبة canvas
   - `python3` — بناء الـ native modules
3. إضافة `DISCORD_TOKEN` و `GOOGLE_API_KEY` كـ Secrets في Replit

---

### 🔴 المشكلة الثانية: SpotifyPlugin يرفع خطأ عند التشغيل

**الخطأ:**
```
Error: SpotifyPlugin: Unknown option "parallel"
Error: SpotifyPlugin: Unknown option "emitEventsAfterFetching"
```

**السبب:** الخيارات دي كانت موجودة في إصدارات قديمة من `@distube/spotify` وتم حذفها في v2.0.2.

**الحل:** حذف الخيارين من إعداد SpotifyPlugin.

---

### 🔴 المشكلة الثالثة: yt-dlp تحذير "No JS runtime available"

**الخطأ:**
```
WARNING: [youtube] Unable to download webpage: No JS runtime available
```

**السبب:** yt-dlp بيحتاج JavaScript runtime لبعض عمليات YouTube، والـ binary مش عارف يلاقي Node.js في بيئة Nix.

**الحل:** إنشاء ملف config لـ yt-dlp يحدد مسار Node.js:
```
# ~/.yt-dlp.conf
--js-runtimes node:/nix/store/.../node
```

---

### 🔴 المشكلة الرابعة: Spotify text search فاشل بسبب Premium

**الخطأ:**
```
403 Forbidden: Active premium subscription required for the owner of the app
```

**السبب:** Spotify Web API `/v1/search` يطلب **Premium subscription** على حساب المطور اللي أنشأ الـ Developer App — وهذا مش في يد أي حد من الفريق.

**التحقيق:**
- كان في كود `spotifySearch()` يجيب token بـ client_credentials ثم يبحث في Spotify API
- حتى بعد إصلاح كل حاجة، الـ Spotify API رفض الطلب بسبب Premium

**الحل:**
1. **حذف** `spotifySearch()` و `getSpotifyToken()` وكل كود Spotify API
2. **حذف** `import https` اللي كان مستخدم معاهم
3. **حذف** `api: { clientId, clientSecret }` من SpotifyPlugin ← ده بيخليه يشتغل بـ **scraping mode** بدون API
4. **تغيير** البحث النصي ليروح مباشرة لـ **YouTube عبر yt-dlp** بدل Spotify
5. النتيجة:
   - ✅ روابط Spotify تشتغل عبر scraping
   - ✅ البحث النصي يشتغل عبر YouTube
   - ✅ مفيش حاجة بتطلب Premium

---

### 🔴 المشكلة الخامسة: زرار "اطلع" بيدي خطأ Discord

**السبب:** Discord بيطلب acknowledge للـ Button Interaction خلال 3 ثواني. الكود القديم كان بيمسح الرسالة من غير ما يعمل acknowledgment أول، فـ Discord بيدي:
```
InteractionAlreadyReplied / Unknown interaction
```

**الحل:**
```javascript
// قبل أي حاجة — acknowledge الـ interaction
await interaction.deferUpdate().catch(() => {});

// بعدين امسح الداش بورد
await interaction.message.delete().catch(() => {});

// بعدين وقف DisTube (بيخرج من الـ voice تلقائياً)
await distube.stop(interaction.guildId);
```

---

## 6. الحالة الحالية لنظام الموسيقى

```
البحث النصي:     YouTube (yt-dlp) → SoundCloud (fallback)
روابط Spotify:   SpotifyPlugin scraping mode ✅
روابط YouTube:   YtDlpPlugin ✅
روابط SoundCloud: SoundCloudPlugin ✅
زرار "اطلع":    deferUpdate → delete message → distube.stop() ✅
الـ Dashboard:   يُرسل مرة واحدة ويُحدَّث ✅
```

---

## 7. نقاط تستاهل الانتباه مستقبلاً

| النقطة | التفاصيل |
|---|---|
| **Spotify Premium** | لو حد من الفريق اشترى Spotify Premium ويربط الـ Developer App بحسابه، ممكن ترجع Spotify text search |
| **yt-dlp updates** | `YtDlpPlugin({ update: false })` — التحديث التلقائي معطل عشان يتحكم فيه يدوياً |
| **Google Drive / Docs** | المتغيرات اختيارية ومش موجودة، ميزات Drive والـ Docs مش شغالة دلوقتي |
| **single-instance.js** | في حماية ضد تشغيل نسختين من البوت في نفس الوقت (instanceId في Replit) |
| **gemini-keys-extra.json** | لو المفاتيح اتحذف من الملف، البوت بيرجع للمفتاح الأساسي في الـ Secrets |

---

## 8. خلاصة الجلسة

| # | المهمة | الحالة |
|---|---|---|
| 1 | نقل المشروع لـ Replit وتثبيت التبعيات | ✅ مكتمل |
| 2 | إصلاح SpotifyPlugin options | ✅ مكتمل |
| 3 | إصلاح yt-dlp JS runtime warning | ✅ مكتمل |
| 4 | إضافة SPOTIFY_CLIENT_ID/SECRET | ✅ مكتمل |
| 5 | تشخيص مشكلة Spotify Premium | ✅ محلول |
| 6 | تحويل البحث النصي لـ YouTube | ✅ مكتمل |
| 7 | تفعيل SpotifyPlugin scraping mode | ✅ مكتمل |
| 8 | إصلاح زرار "اطلع" (deferUpdate) | ✅ مكتمل |

**حالة البوت الآن:** 🟢 شغال — 59 أمر مرفوع — نظام موسيقى كامل

---

*آخر تحديث: 25 يونيو 2026*
