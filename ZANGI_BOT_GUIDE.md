# 🤖 شرح كامل — بوت زنجي (Zangi Bot)

> **ملاحظة:** الشرح ده مكتوب بالعربي المصري ومتقسم على أجزاء واضحة. كل جزء بيشرح ميزة أو نظام كامل في البوت.

---

## 📌 معلومات عامة

| البيان | التفاصيل |
|--------|----------|
| **اسم البوت** | زنجي (Zangi) |
| **اللغة** | Node.js — ES Modules |
| **المكتبة الرئيسية** | discord.js v14/v15 |
| **الذكاء الاصطناعي** | Google Gemini API |
| **قاعدة البيانات** | JSON محلي (`server_database.json`) |
| **عدد الأوامر** | ~73 أمر slash |
| **نقطة البداية** | `index.js` (الملف الرئيسي — ~4500 سطر) |

---

## 🗂️ هيكل الملفات

```
📁 بوت زنجي/
│
├── index.js                ← 🧠 المخ الرئيسي — كل الأحداث والروتنج هنا
├── config.js               ← ⚙️ إعدادات البوت (التحقق من المتغيرات، IDs الأونر)
├── database.js             ← 🗄️ التعامل مع قاعدة البيانات JSON
├── logger.js               ← 📝 تسجيل اللوجز
│
├── 📁 commands/            ← كل أوامر الـ slash
│   ├── games.js            ← روليت + مافيا + اكس-اوه + ح.و.م
│   ├── games-hub.js        ← مركز الألعاب + أحدث المميزات + طريقة الكلام
│   ├── bank-luck-eg.js     ← بنك الحظ المصري (العجلة)
│   ├── bank-life.js        ← لعبة الحياة (8 مواقف)
│   ├── codenames.js        ← لعبة كود نيمز
│   ├── party-games.js      ← الهاتف المكسور + صنع الميم
│   ├── quiz.js             ← لعبة المسابقة
│   ├── polls.js            ← نظام الاستفتاءات
│   ├── game-shop.js        ← متجر القدرات
│   ├── daily-challenge.js  ← التحديات الأسبوعية
│   ├── music.js            ← المشغل الموسيقي
│   ├── image-cleaner.js    ← تنظيف صفحات المانجا
│   ├── quick-clean.js      ← تنظيف سريع بالـ OCR
│   └── translator.js       ← ترجمة فصول المانجا
│
├── 📁 helpers/             ← أنظمة الخلفية
│   ├── auto-mod.js         ← نظام المراقبة الذكية
│   ├── gemini-keys.js      ← إدارة مفاتيح Gemini وتبديلها
│   ├── owner-ai.js         ← لوحة تحكم الأونر بالذكاء الاصطناعي
│   └── rank-roles.js       ← نظام أدوار المستويات التلقائية
│
├── 📁 data/                ← ملفات بيانات دائمة
│   ├── bot-memory.json     ← ذاكرة المحادثات القصيرة
│   └── gemini-keys-extra.json ← مفاتيح Gemini الإضافية
│
├── server_database.json    ← قاعدة بيانات المستخدمين والسيرفر
└── welcome.png             ← صورة الترحيب الافتراضية
```

---

## 👤 نظام المستخدمين وقاعدة البيانات

### هيكل بيانات كل مستخدم:

```json
{
  "userId": {
    "xp": 500,
    "level": 3,
    "coins": 1200,
    "bankCoins": 5000,
    "lastDaily": 1718000000000,
    "lastBankInterest": 1718000000000,
    "warnings": [
      { "reason": "سبب التحذير", "mod": "modId", "date": "2024-01-01" }
    ],
    "gameAbilities": {
      "nuke": 2,
      "extraLife": 1
    }
  }
}
```

### كلاس `Database` (database.js):

| الميثود | الوظيفة |
|---------|---------|
| `getUser(id)` | جيب أو أنشئ بيانات المستخدم |
| `updateUser(id, data)` | حدّث بيانات المستخدم |
| `addCoins(id, amount)` | أضف كوينز |
| `addXP(id, amount)` | أضف XP وتحقق من اللفل أپ |
| `addWarning(id, data)` | أضف تحذير |
| `getWarnings(id)` | جيب كل تحذيرات مستخدم |
| `useGameAbility(id, type)` | استخدم قدرة وامسحها لو وصلت للصفر |
| `save()` | احفظ الـ JSON على الديسك |

---

## 💰 نظام الاقتصاد والـ XP

### كيف يكسب المستخدم XP؟
- كل رسالة يبعتها في السيرفر = **+5 XP** تلقائي
- معادلة اللفل: `Level = floor(sqrt(XP / 50))`

### جدول تقريبي:
| اللفل | XP المطلوب |
|-------|-----------|
| 1 | 50 |
| 5 | 1,250 |
| 10 | 5,000 |
| 20 | 20,000 |
| 50 | 125,000 |

### الكوينز (العملة):
- تُكسب من: `/يومي` + الألعاب + المسابقات + التحديات الأسبوعية
- تُصرف في: `/متجر` (أدوار) + `/متجر-قدرات` (قدرات الألعاب)

---

## 📋 كل الأوامر مقسّمة بالتفصيل

### 🟢 أوامر عامة

| الأمر | الوظيفة |
|-------|---------|
| `/زنجي [رسالة]` | دردشة مع الـ AI (Gemini) |
| `/مساعدة` | قائمة بكل الأوامر |
| `/ping` | فحص سرعة البوت |
| `/serverinfo` | معلومات السيرفر |
| `/userinfo [@مستخدم]` | معلومات مستخدم |
| `/حالة-البوت` | إحصائيات أداء البوت |
| `/اقتراح` | لوحة اقتراحات/مشاكل/تعليقات |
| `/القوانين` | قوانين السيرفر |
| `/صورة` | اشرح صورة بالـ AI |

### 💛 أوامر الاقتصاد

| الأمر | الوظيفة |
|-------|---------|
| `/بروفايل [@مستخدم]` | عرض اللفل والـ XP والكوينز والبنك |
| `/محفظة` | عرض الكوينز والـ XP بسرعة |
| `/يومي` | مكافأة يومية (cooldown 24 ساعة) |
| `/ليدربورد [نوع]` | أفضل 10 مستخدمين بالكوينز أو XP |
| `/متجر` | شراء أدوار مدفوعة (ذهبي/فضي/برونزي) |
| `/متجر-قدرات` | شراء قدرات خاصة للألعاب |
| `/قدراتي` | عرض القدرات اللي عندك |
| `/إعطاء [@مستخدم] [مبلغ]` | (أدمن) إعطاء كوينز |

### 🛡️ أوامر المودريشن

| الأمر | الوظيفة | يحتاج إذن |
|-------|---------|-----------|
| `/تحذير @مستخدم [سبب]` | تحذير عضو | مشرف |
| `/اسكات @مستخدم [دقائق] [سبب]` | إسكات مؤقت | مشرف |
| `/طرد @مستخدم [سبب]` | طرد عضو (مع تأكيد) | مشرف |
| `/تبنيد @مستخدم [سبب]` | بان عضو (مع تأكيد) | أدمن |
| `/مسح [عدد]` | مسح رسائل (حتى 10,000) | مشرف |
| `/مسح-الكل` | مسح روم كامل | أدمن |
| `/تحذيرات [@مستخدم]` | عرض تحذيرات عضو | مشرف |
| `/auto-mod` | تشغيل/إيقاف نظام المراقبة | أدمن |
| `/انشاء-رول [اسم] [نوع] [لون]` | إنشاء رول ذكي | أدمن |
| `/تعديل-رول` | تعديل رول موجود | أدمن |
| `/رفع-بلوك` | رفع البلوك عن عضو | أدمن |
| `/قائمة-مبلوكين` | قائمة المبلوكين | أدمن |

> **ملاحظة:** `/طرد` و `/تبنيد` بيعرضوا أزرار تأكيد قبل التنفيذ. المودريتور اللي أصدر الأمر بس هو اللي يقدر يؤكد.

### 🎮 أوامر الألعاب

| الأمر | الوظيفة |
|-------|---------|
| `/الألعاب` | مركز الألعاب — كل الألعاب من مكان واحد |
| `/روليت` | روليت روسية |
| `/مافيا` | لعبة المافيا |
| `/اكس-اوه [@خصم]` | تيك تاك تو (اتركه فاضي للعب ضد AI) |
| `/بنك-الحظ-مصري` | عجلة الحظ المصرية |
| `/لعبة-الحياة` | 8 مواقف حياة واقعية |
| `/كود-نيمز` | لعبة كود نيمز |
| `/الهاتف-المكسور` | الهاتف المكسور |
| `/صنع-الميم` | صنع الميم |
| `/استفتاء` | إنشاء استفتاء |
| `/تحدي-يومي` | التحديات الأسبوعية |

### 🎵 أوامر الموسيقى

| الأمر | الوظيفة |
|-------|---------|
| `/play [رابط/اسم]` | تشغيل أغنية من YouTube/Spotify |
| `/skip` | تخطي الأغنية الحالية |
| `/stop` | إيقاف الموسيقى والخروج |
| `/queue` | عرض قائمة الأغاني |
| `/pause` | إيقاف مؤقت |
| `/resume` | استكمال |
| `/volume [0-100]` | التحكم في الصوت |

### 🖼️ أوامر مانهوا/مانجا

| الأمر | الوظيفة |
|-------|---------|
| `/clean_chapter` | تنظيف صفحات مانجا من الفقاعات |
| `/translate_chapter` | ترجمة فصل مانجا بالـ OCR + Gemini |
| `/manhwa-إنشاء` | إنشاء قاموس مانهوا |

### 👑 أوامر الأونر فقط

| الأمر | الوظيفة |
|-------|---------|
| `/تغيير-طريقة-الكلام [أسلوب]` | غيّر شخصية البوت (محترم/حر/توكسيك) |
| `/مفاتيح-جيميني` | إدارة مفاتيح Gemini |
| `/نسخة-احتياطية` | إنشاء نسخة احتياطية يدوية |
| `/استرجاع` | استرجاع نسخة احتياطية |
| `/رسالة-جماعية` | إرسال رسالة جماعية |

---

## 🎮 شرح كل لعبة بالتفصيل

### 🔫 روليت روسية
- **عدد اللاعبين:** 2 إلى 8
- **الفكرة:** كل واحد بيضغط الزناد، الرصاصة بتتحرك بين اللاعبين
- **الفوز:** آخر لاعب يفضل على قيد الحياة يكسب كل الكوينز المراهن عليها
- **القدرات:** `extraLife` (حياة إضافية) — `nuke` (تفجير لاعب)

### 🕵️ مافيا
- **عدد اللاعبين:** 4 إلى 12
- **الأدوار:** مافيا / طبيب / محقق / مدني
- **الأدوار بتتبعت في الـ DM** سرية
- **الفوز:** المدنيون يكشفوا المافيا، أو المافيا تقضي على المدنيين

### ❌⭕ اكس-اوه (Tic-Tac-Toe)
- **ضد لاعب:** `/اكس-اوه @خصم` — دعوة + قبول
- **ضد AI 🤖:** `/اكس-اوه` (من غير خصم) أو زرار "🤖 اكس-اوه ضد AI" في الهب
- **الـ AI:** يستخدم خوارزمية **Minimax مع Alpha-Beta Pruning** — لا يُهزم!
- **الفوز ضد AI:** **200 كوينز** للاعب لو كسب
- **الفوز ضد لاعب:** **150 كوينز** للفايز

### 🎰 بنك الحظ المصري
- **الفكرة:** كرتونة بنك الحظ الأصلية — بتراهن بكوينزك والعجلة بتلف
- **العجلة:** 10 أرقام من 0x (إفلاس) إلى 10x (جاكبوت)
- **الاحتمالات:** الأرقام العالية أصعب، الأرقام المنخفضة أكثر

| الرقم | المكافأة | الاحتمال |
|-------|---------|---------|
| 1-2 | 💀 خسارة كل حاجة | 40% |
| 3-4 | 😢 نص الرهان | 25% |
| 5-6 | 😐 رجوع الرهان | 16% |
| 7 | 😄 ضعف الرهان | 6% |
| 8 | 🔥 3 أضعاف | 5% |
| 9 | 💎 5 أضعاف | 5% |
| 10 | 🎉 10 أضعاف (جاكبوت) | 3% |

### 🌍 لعبة الحياة
- **عدد اللاعبين:** 1 إلى 8
- **الفكرة:** 8 مواقف حياة واقعية بتظهر للكل في نفس الوقت
- **الوقت:** 20 ثانية لكل موقف تختار من 3 خيارات
- **الخيارات:** جريء (نقاط عالية + خطر) / متوازن / آمن (نقاط أقل)
- **الفوز:** أعلى نقاط في الآخر يكسب **400 كوينز**

### 🃏 كود نيمز
- **فريقين:** أحمر وأزرق
- **كل فريق عنده:** Spymaster (بيعرف الكلمات السرية) + اللاعبين
- **الـ Spymaster** يبعت تلميح كلمة واحدة + رقم
- **اللاعبين** يخمنوا الكلمات اللي بتنتمي لفريقهم
- **الخسارة الفورية:** لو ضغطوا على الكلمة القاتلة (💀)

### 📞 الهاتف المكسور
- لاعب يكتب جملة → التاني يوصفها بصورة → التالت يخمن الجملة → ضحك مضمون!

### 😂 صنع الميم
- البوت يختار GIF أنيمي/جيمنج عشوائي — كل لاعب يكتب كابشن عليه
- الكل يصوّت — الفايز يكسب **200 كوينز**

### 🧠 مسابقة (Quiz)
- أسئلة ثقافية في الروم
- أول واحد يجاوب صح يكسب **100 كوينز + 30 XP**

---

## 🤖 نظام الـ AI (Gemini)

### إزاي البوت بيتكلم:
1. المستخدم يكتب في الروم أو يمنشن البوت أو يستخدم `/زنجي`
2. البوت يبعت الرسالة لـ Gemini مع السياق (آخر 10 رسائل)
3. الـ AI يرد بشخصية محددة حسب الـ speech mode

### أوضاع الكلام (Speech Mode):
| الوضع | الوصف |
|-------|-------|
| 🎩 محترم (normal) | رد لطيف ومودب، مفيش شتايم |
| 😈 حر (free) | لو حد شتمه يرد بنفس الشتيمة |
| ☠️ توكسيك (toxic) | شتايم في أغلب كلامه |

> يتغير بـ `/تغيير-طريقة-الكلام` (أونر فقط)

### نظام تبديل المفاتيح (gemini-keys.js):
- البوت بيدعم **مفاتيح Gemini متعددة**
- لو مفتاح وصل الـ rate limit، ينتقل تلقائي للمفتاح التاني
- المفاتيح الإضافية بتتحفظ في `data/gemini-keys-extra.json`
- المفتاح الأساسي دايماً من `GOOGLE_API_KEY`

### الـ AI بيشوف الصور:
- لو حد بعت صورة في الروم، البوت يقدر يحللها ويوصفها
- يستخدم **Gemini Vision** (multimodal)

---

## 🛡️ نظام Auto-Mod (المراقبة الذكية)

### ازاي بيشتغل:
```
رسالة جديدة
    ↓
Pass 1: EXTREME_REGEX
    ↓ (لو اتعرّف)
مسح فوري + لوج في روم المشرفين
    ↓ (لو مش extreme)
Pass 2: SUSPICIOUS_REGEX
    ↓ (لو مشبوه)
Gemini يحلل النص (throttle 5 ثواني)
    ↓
تنفيذ الأكشن
```

### الأكشنز الممكنة:
1. **مسح الرسالة** فوراً
2. **لوج في روم المشرفين** مع 3 أزرار: استرجاع / بان / تحذير
3. **Anti-Spam:** 5+ رسائل في 10 ثواني → تحذير → إسكات 5 دقائق → إسكات 30 دقيقة → طرد
4. **Anti-Link:** حذف اللينكات من domain مش في الـ whitelist
5. **Anti-Raid:** لو 10+ عضو انضموا في 30 ثانية → قفل السيرفر + تنبيه الأدمن

---

## 🎖️ نظام أدوار المستويات (rank-roles.js)

بيتشغل تلقائي لما المستخدم يطلع لفل:

| اللفل | الرول |
|-------|-------|
| 5 | 🥉 برونزي |
| 10 | 🥈 فضي |
| 20 | 🥇 ذهبي |
| 50 | 👑 أسطوري |

> الأرقام دي قابلة للتعديل في `helpers/rank-roles.js`

---

## 🏪 متجر القدرات (game-shop.js)

| القدرة | الوظيفة | السعر |
|--------|---------|-------|
| 💣 Nuke | تفجير لاعب في الروليت | 500 كوينز |
| 🛡️ Extra Life | حياة إضافية في الروليت | 300 كوينز |
| ⚡ Double Kick | ضربتين في جولة واحدة | 400 كوينز |

- القدرات بتتخزن في `user.gameAbilities`
- لما تستخدمها بتتمسح تلقائي لو العدد وصل للصفر

---

## 🔑 المتغيرات المطلوبة (Environment Variables)

| المتغير | مطلوب؟ | الوظيفة |
|---------|--------|---------|
| `DISCORD_TOKEN` | ✅ إلزامي | توكن البوت من Discord Developer Portal |
| `GOOGLE_API_KEY` | ✅ إلزامي | مفتاح Gemini API من Google AI Studio |
| `GOOGLE_CLIENT_ID` | ⚠️ اختياري | للتكامل مع Google Drive/Docs |
| `GOOGLE_CLIENT_SECRET` | ⚠️ اختياري | للتكامل مع Google Drive/Docs |
| `GOOGLE_REFRESH_TOKEN` | ⚠️ اختياري | للتكامل مع Google Drive/Docs |
| `YOUTUBE_API_KEY` | ⚠️ اختياري | لتحسين البحث في الموسيقى |

---

## 🔄 دورة الحياة — ازاي البوت بيشتغل؟

```
node index.js
    ↓
1. تحميل إعدادات config.js (التحقق من المتغيرات)
2. تحميل قاعدة البيانات (server_database.json)
3. تشغيل Express Server (port 3000) — للـ keep-alive
4. تسجيل الدخول بـ Discord Token
5. رفع 73 أمر slash على Discord
6. تشغيل نظام النسخ الاحتياطية التلقائية (كل يوم)
7. جدولة التحديات الأسبوعية (كل جمعة 1 ظهر)
8. البوت جاهز ✅
    ↓
الأحداث:
├── messageCreate → XP + Auto-Mod + AI Chat
├── interactionCreate → slash commands + buttons + modals
├── guildMemberAdd → رسالة ترحيب + منع الـ duplicates
└── voiceStateUpdate → مراقبة الـ voice channels
```

---

## 📊 الأنظمة الثانوية المهمة

### 🔒 نظام تأكيد المودريشن:
- أوامر الطرد والبان بتحتاج تأكيد
- بيظهر embed مع زرار "تأكيد" و"إلغاء"
- **مهلة الـ 90 ثانية** — بعدها الأمر بيتلغي تلقائي
- المودريتور الأصلي بس هو اللي يقدر يؤكد

### 💾 نظام النسخ الاحتياطية:
- نسخة احتياطية يومية تلقائية لـ `server_database.json`
- يقدر الأونر يعملها يدوي بـ `/نسخة-احتياطية`
- يقدر يسترجع بـ `/استرجاع`

### 📬 نظام الـ Keep-Alive:
- Express server شغال على port 3000
- البوت بيعمل ping لنفسه كل دقيقتين عشان ما يوقفش

### 🏆 التحديات الأسبوعية:
- بتتبعت تلقائياً كل جمعة الساعة 1 ظهر
- مش بتتبعت عند ريستارت البوت (مخزون وقت آخر تحدي)

---

## 🧠 خوارزمية Minimax في اكس-اوه

البوت بيستخدم **Minimax مع Alpha-Beta Pruning** في لعبة اكس-اوه ضد الـ AI:

```
الـ AI (O) = Maximizer → بيدور على أعلى نقطة
اللاعب (X) = Minimizer → بيدور على أقل نقطة

نظام النقاط:
+10 = AI كسب
-10 = اللاعب كسب
 0  = تعادل

Alpha-Beta: بيقطع الفروع اللي مش هتأثر في النتيجة → أسرع بكتير
```

> **النتيجة:** الـ AI مش ممكن يخسر — أفضل نتيجة ممكنة للاعب هي التعادل.

---

## 👑 IDs الأونر

```javascript
// في config.js
OWNER_IDS: [
  '954816748140503090',   // مالك
  '1448840687763325018',  // مصطفى
  '844490366614110218',   // عمر
  '594250288282730551',   // أوفيكس
]
```

الأونر يقدر يعمل:
- تغيير شخصية البوت
- إدارة مفاتيح Gemini
- نسخ احتياطية
- رسائل جماعية
- لوحة DM للتحكم في السيرفر عن بعد (Owner AI)

---

## ⚠️ قواعد مهمة للمطورين

1. **أي أمر جديد** لازم يتضاف في `LATEST_FEATURES` في `commands/games-hub.js`
2. **الـ gameId** مش بيتضمّن `_` في النص عشان السبليت ما يتعطلش
3. **أي لعبة جديدة** محتاجة `channelGames` عشان تمنع لعبتين في نفس الروم
4. **المودالات** مش بتستخدم `interaction.update()` — لازم `fetchMessage()` ثم `edit()`
5. **كود نيمز** عنده حد 5 ActionRows — الأزرار الإضافية بتتبعت كرسالة منفصلة

---

*آخر تحديث: يونيو 2026*
