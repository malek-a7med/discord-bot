[phases.setup]
nixPkgs = ["nodejs_22", "ffmpeg", "python3", "vips", "pkg-config"]

[phases.install]
cmds = [
  "npm install --no-audit --no-fund --ignore-scripts",
  "npm rebuild sharp --no-audit",
]

[phases.build]
cmds = []

[start]
cmd = "npm run start"
