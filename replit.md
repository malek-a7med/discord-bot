# نظرة عامة على المشروع

بوت ديسكورد (Node.js + discord.js) بيدعم أوامر سلاش كتير: ألعاب (رولت، مافيا، إكس أو، كودنيمز، إلخ)، نظام موسيقى (@discordjs/voice + play-dl)، ميزات ذكاء اصطناعي (Gemini)، نظام مودريشن ذكي، بولز، وغيرها. نقطة الدخول `index.js`، منطق قاعدة البيانات في `database.js`، الأوامر في مجلد `commands/`.

## تشغيل المشروع

- Workflow "Start application": `node index.js`
- Workflow "JMusicBot": `bash jmusicbot/start.sh`

المشروع لسه محتاج توكنات/سيكرتس (زي توكن الديسكورد بوت ومفاتيح Gemini) قبل ما يتشغل فعليًا.

## تفضيلات المستخدم (User preferences)

- المستخدم عايز كل التواصل يكون باللهجة المصرية بس، ومفيش ولا حرف انجليزي خالص في الردود.
- المستخدم عايز الردود سريعة ومن غير مجهود زيادة أو تفاصيل مطولة غير لازمة.
